var locker = ["book","phone","keys"]

//locker.sort();
//locker.reverse();
//console.log(locker);

console.log(locker[0]);
console.log(locker[1]);
console.log(locker[2]);
var lastlocker = locker[locker.length-1];
console.log(lastlocker);

locker.push("hats");
locker.pop();
console.log(locker);

var numberOflocker = locker.length;
console.log(numberOflocker);